# OpenDog Android 第一阶段制作指导

## 一、第一阶段目标

先跑通最小链路：

```text
识别当前应用和页面
        ↓
提取无障碍页面文字
        ↓
生成 focus_switch 事件
        ↓
保存到手机本地
        ↓
上传服务器 /ingest
```

完成后，手机切换应用时，服务器能够收到一条包含应用、页面和文字内容的事件。

手机端事件格式需要与现有电脑端和服务端保持兼容。

---

## 二、第一阶段暂时不做

暂时不要加入：

* 截图和 OCR
* 点击、滚动和输入分析
* `interaction_idle`
* 位置和通知采集
* 输入法
* WorkManager 自动补传
* 本地 AI 总结
* 图片上传

第一阶段只处理应用或页面切换事件。

---

## 三、开发前准备

### 1. 准备 Android 真机

完成：

* 开启开发者选项
* 开启 USB 调试
* Android Studio 能识别手机
* 能安装并运行空白 App
* Logcat 能看到手机日志

### 2. 验证服务器接口

先在电脑上手工请求：

```text
POST http://服务器地址:8899/ingest
Authorization: Bearer <token>
```

确保服务器返回：

```json
{
  "ok": true
}
```

服务器接口没有测试成功前，不开始手机端上传功能。

---

## 四、建立最小工程结构

建议至少拆成以下模块：

```text
MainActivity
负责权限、服务器配置和状态显示

OpenDogAccessibilityService
负责监听应用和窗口变化

PageSnapshotExtractor
负责提取包名、页面类名和页面文字

EventBuilder
负责生成标准事件

EventRepository
负责保存本地事件

IngestClient
负责上传 /ingest
```

不要把所有代码写在一个 Activity 或 Service 中。

---

## 五、实现无障碍采集

### 1. 引导用户开启权限

主界面提供：

```text
无障碍服务：已开启 / 未开启
打开无障碍设置
```

用户必须在系统设置中手动开启。

### 2. 监听主要事件

第一阶段只重点处理：

* 窗口状态变化
* 窗口内容变化

### 3. 获取当前页面

每次窗口变化后读取：

* `packageName`
* `className`
* `windowId`
* 页面标题
* 当前活动窗口根节点

第一步先只验证包名和类名是否正确。

例如：

```text
com.android.chrome
ChromeTabbedActivity
```

---

## 六、提取页面文字

遍历当前活动窗口的无障碍节点，读取：

* `text`
* `contentDescription`

然后进行简单整理：

1. 删除空文字。
2. 删除重复文字。
3. 压缩多余空格。
4. 按页面顺序使用换行拼接。
5. 限制最大长度，例如 10,000 个字符。

如果某个 App 无法提取文字，允许：

```text
text = ""
```

不要在第一阶段立即增加 OCR。

---

## 七、判断页面切换

第一阶段使用：

```text
focus_id = packageName/className
```

例如：

```text
com.android.chrome/ChromeTabbedActivity
```

收到页面变化后：

1. 保存候选 `focus_id`。
2. 等待约 300～500 毫秒。
3. 再读取一次当前页面。
4. 两次 `focus_id` 相同，认为页面稳定。
5. 生成一条 `focus_switch` 事件。

维护一个 `current_focus_id`：

* 新旧不同：生成事件。
* 新旧相同：不重复生成。

必须排除 OpenDog 自身包名，避免循环采集。

---

## 八、生成标准事件

每次页面切换生成：

```json
{
  "event_id": "UUID",
  "type": "focused_window_ocr",
  "ts": 1783502400.123,
  "data": {
    "trigger": "focus_switch",
    "focus_id": "com.android.chrome/ChromeTabbedActivity",
    "app": "com.android.chrome",
    "title": "页面标题",
    "text": "页面文字",
    "capture_method": "accessibility"
  }
}
```

关键要求：

* 每条事件使用新的 UUID。
* `ts` 直接使用手机生成的 Unix 时间戳。
* `app` 使用 Android 包名。
* `capture_method` 固定为 `accessibility`。
* 第一阶段 `trigger` 固定为 `focus_switch`。

---

## 九、先保存，再上传

使用 Room 建立最小事件表，保存：

| 字段           | 作用      |
| ------------ | ------- |
| `event_id`   | 唯一事件 ID |
| `type`       | 事件类型    |
| `ts`         | 时间戳     |
| `data_json`  | 事件内容    |
| `status`     | 上传状态    |
| `last_error` | 最近错误    |

状态只需要：

```text
PENDING
SENT
FAILED
```

处理顺序必须是：

```text
生成事件
→ 保存为 PENDING
→ 尝试上传
→ 成功后改为 SENT
```

断网时事件仍保留为 `PENDING`。

---

## 十、上传服务器

请求格式：

```json
{
  "source": "android_a",
  "device_id": "android_phone_001",
  "events": [
    {
      "event_id": "UUID",
      "type": "focused_window_ocr",
      "ts": 1783502400.123,
      "data": {}
    }
  ]
}
```

请求头：

```text
Authorization: Bearer <token>
Content-Type: application/json
```

处理规则：

| 情况        | 处理               |
| --------- | ---------------- |
| `ok=true` | 标记为 `SENT`       |
| 断网或超时     | 保持 `PENDING`     |
| 401 / 403 | 标记失败并提示 Token 错误 |
| 400 / 422 | 保存服务器错误          |
| 409 或重复事件 | 按成功处理            |

第一阶段提供一个“重新上传”按钮即可，不需要自动后台重试。

---

## 十一、主界面最少显示内容

主界面建议显示：

```text
无障碍服务状态

服务器地址
Token
device_id

当前应用包名
当前页面类名
当前 focus_id
提取文字长度
部分页面文字

本地待上传数量
最近上传结果
最近服务器错误
```

这样可以快速判断问题出现在：

* 权限
* 页面识别
* 文字提取
* 本地存储
* 网络上传

---

## 十二、推荐制作顺序

严格按照以下顺序开发：

```text
1. 真机连接 Android Studio
2. 手工验证服务器 /ingest
3. 创建主界面
4. 注册 AccessibilityService
5. 获取前台包名和类名
6. 提取页面文字
7. 实现 300～500 毫秒页面稳定判断
8. 生成 focus_switch 事件
9. 使用 Room 保存事件
10. 实现 /ingest 上传
11. 增加手动重新上传
12. 完成断网和重复上传测试
```

---

## 十三、第一阶段验收标准

第一阶段完成时必须满足：

* 能正确开启无障碍服务。
* 切换 App 后能获取包名和页面类名。
* 普通页面能提取主要文字。
* 页面切换只生成一条事件。
* OpenDog 不会采集自身。
* 事件先保存到本地。
* 断网时事件不会丢失。
* 恢复网络后可以手动补传。
* 服务器收到 `source=android_a`。
* 服务器收到正确 `device_id`。
* 重复上传不会产生重复数据。

第一阶段完成后的数据流为：

```text
AccessibilityService
        ↓
PageSnapshotExtractor
        ↓
页面稳定判断
        ↓
EventBuilder
        ↓
Room 本地事件
        ↓
IngestClient
        ↓
服务器 /ingest
```
五、建立 Android 工程结构

第一阶段虽然功能少，但不要把所有逻辑写进一个 Activity 或一个 Service 文件。

建议至少分成以下模块。

app
├── ui
│   └── MainActivity
│
├── accessibility
│   ├── OpenDogAccessibilityService
│   ├── PageSnapshotExtractor
│   └── PageStabilityController
│
├── event
│   ├── PageSnapshot
│   ├── OpenDogEvent
│   └── EventBuilder
│
├── storage
│   ├── EventEntity
│   ├── EventDao
│   └── EventRepository
│
├── network
│   ├── IngestRequest
│   ├── IngestResponse
│   └── IngestClient
│
├── config
│   └── AppConfig
│
└── logging
    └── AppLogger

各模块职责如下。

MainActivity

负责：

显示权限状态；
引导用户打开无障碍设置；
配置服务器地址和 Token；
显示当前识别页面；
显示最近事件；
显示上传状态。
OpenDogAccessibilityService

负责：

接收系统无障碍事件；
判断事件来自哪个应用；
获取当前活动窗口；
把窗口交给页面提取模块；
不直接负责网络上传。
PageSnapshotExtractor

负责：

遍历页面节点；
提取包名、类名、标题和文字；
生成统一的页面快照。
PageStabilityController

负责：

页面切换后的延迟确认；
防止同一页面短时间重复触发；
维护当前 focus_id。
EventBuilder

负责：

生成 UUID；
生成时间戳；
把页面快照转换成服务器事件。
EventRepository

负责：

保存事件；
查询未上传事件；
更新上传结果。
IngestClient

负责：

调用 /ingest；
添加 Bearer Token；
解析服务器响应。